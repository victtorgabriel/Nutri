# Versão original

Configurações originais, preservadas como referência. A pasta `node_modules` contém as dependências da instalação anterior. O código React permanece em `../src/`, pois o Windows bloqueou a movimentação dessa pasta.

Esses arquivos não são usados pelo site atual, que está em `../frontend/index.html`.
